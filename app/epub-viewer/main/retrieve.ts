import type { Unzipped } from 'fflate'

/**
 * try retrieving epub from:
 * + url in broswer's address bar
 * + the file which emit the startup of this pwa
 */
export
function retrieve_file(): Unzipped | null {
  return null
}
